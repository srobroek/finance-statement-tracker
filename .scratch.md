Assigned production-cutover repair on base 2a9f825.

Evidence anchors:
- .orchestration/run-20260820-finance-mvp/artifacts/orc-partial-cutover-recovery-plan-r1.md lists 33 deterministic references, seven absent legacy tables, the protected 19-workflow export contract, and same-lock rollback.
- .orchestration/run-20260820-finance-mvp/artifacts/orc-partial-cutover-recovery-plan-r2.json contains the 33-ID action map and lock identity.

Implementation notes:
- Derive and verify action IDs from data-table-migration-matrix.json rather than duplicating node metadata.
- Require a mode-0600, integrity-sealed live export and quiescence receipt for production-preflight/forward shell mutation.
- Use atomic durable receipt writes plus an exclusive file lock and re-check source/generator/export currentness around every mutation boundary.

Rebase run:
- Target a9818ef3e9687168bb4753795deb05f0b83fab20; replay commit 4905b0e, then refresh generated matrix provenance in 8f614e6.
- Remote head: 8f614e654bcd24604635506fa7e2fdeec505f2f0.
- Focused cutover plus migration-matrix tests: 21 passed.
- Full suite: 7 baseline failures (restore SIGTERM, Microsoft OAuth assertion, three workflow fingerprint drifts, and two missing TypeScript dependency checks); no cutover failure.
- No live runtime mutation.

Review repair:
- Added a separate PRODUCTION_ONLY shell entrypoint that runs preflight, passes protected export and lock receipts into n8n, executes the in-transaction project advisory lock operation, persists the runtime receipt, and requires a forward replay no-op.
- Added n8n CLI runtime implementation for exact live workflow graph/revision binding, target-ID readback, all-33 forward/rollback actions, compensation on failure, and lock/export integrity binding.
- Added static production surface and JavaScript syntax tests; focused tests pass after committing the changes.
- Review follow-up commit 69b72b3 binds migration and accepted-identity digests into the runtime lock environment and CJS validation.
- Verification: focused cutover/migration tests 25 passed; semgrep (Python/JS) 0 findings; bash -n, node --check, py_compile, ruff, PR59 byte diff clean. Trivy completed with existing uv pypdf CVEs and Dockerfile policy findings outside scope.
- Final follow-up commit f6a0d75 binds rollback runtime receipts to the current project lock resource and every graph action's revision/canonical target ID; focused cutover and matrix tests remain 25 passed.
