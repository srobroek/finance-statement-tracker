{
  "id": "10000000-0000-4000-8000-000000000018",
  "name": "Finance · Fenced Actual Writer Lease",
  "active": false,
  "nodes": [
    {
      "id": "18001",
      "name": "Trusted Lease Request",
      "type": "n8n-nodes-base.executeWorkflowTrigger",
      "typeVersion": 1.1,
      "position": [
        -1120,
        0
      ],
      "parameters": {
        "inputSource": "passthrough"
      }
    },
    {
      "id": "18002",
      "name": "Validate Fixed Lease Operation",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -820,
        0
      ],
      "parameters": {
        "jsCode": "// Purpose: Validate Fixed Lease Operation. Keep this deterministic and fail closed.\nconst r = $json, op = String(r.operation || '');\nif (!['ACQUIRE', 'ASSERT', 'RELEASE'].includes(op))\n    throw new Error('INVALID_LEASE_OPERATION');\nif (!/^actual:[A-Za-z0-9_-]{1,128}$/.test(String(r.resource_key || '')))\n    throw new Error('INVALID_LEASE_RESOURCE');\nconst leaseClass = String(r.lease_class || '');\nif (!['ACTUAL_OUTBOX', 'MAINTENANCE'].includes(leaseClass))\n    throw new Error('INVALID_LEASE_CLASS');\nif (op === 'ACQUIRE') {\n    if (!/^n8n:[A-Za-z0-9:_-]{1,160}$/.test(String(r.lease_owner || '')))\n        throw new Error('INVALID_LEASE_OWNER');\n    const ttl = Number(r.ttl_seconds || 120);\n    if (!Number.isInteger(ttl) || ttl < 30 || ttl > 600)\n        throw new Error('INVALID_LEASE_TTL');\n    if (leaseClass === 'MAINTENANCE')\n        return [{ json: { ...r, lease_class: leaseClass, ttl_seconds: ttl } }];\n    if (!/^[A-Za-z0-9:_-]{1,160}$/.test(String(r.outbox_id || '')))\n        throw new Error('INVALID_WRITER_OUTBOX_ID');\n    if (!['PREPARED', 'ACTUAL_OBSERVED', 'VERIFIED', 'RECONCILED', 'COMMITTED'].includes(String(r.outbox_state || '')))\n        throw new Error('WRITER_STATE_NOT_ADMISSIBLE');\n    if (!Number.isSafeInteger(r.attempt_count) || Number(r.attempt_count) < 0)\n        throw new Error('INVALID_WRITER_ATTEMPT_COUNT');\n    const admission = String(r.admission || '');\n    if (!['INITIAL', 'SUCCESSOR'].includes(admission))\n        throw new Error('INVALID_WRITER_ADMISSION');\n    if ((admission === 'INITIAL' && (String(r.outbox_state || '') !== 'PREPARED' || Number(r.attempt_count) !== 0))\n        || (admission === 'SUCCESSOR' && String(r.outbox_state || '') === 'PREPARED' && Number(r.attempt_count) === 0))\n        throw new Error('WRITER_ADMISSION_STATE_MISMATCH');\n    if (!/^[A-Za-z0-9:_-]{1,160}$/.test(String(r.account_id || ''))\n        || !/^[A-Za-z0-9_-]{1,128}$/.test(String(r.budget_id || ''))\n        || String(r.resource_key) !== `actual:${r.budget_id}`)\n        throw new Error('INVALID_WRITER_TARGET_CORRELATION');\n    const periodStart = String(r.period_start || ''), periodEnd = String(r.period_end || '');\n    if (!/^\\d{4}-\\d{2}-\\d{2}$/.test(periodStart) || !/^\\d{4}-\\d{2}-\\d{2}$/.test(periodEnd)\n        || periodStart > periodEnd)\n        throw new Error('INVALID_WRITER_PERIOD');\n    if (!/^[a-f0-9]{64}$/i.test(String(r.payload_sha256 || '')))\n        throw new Error('INVALID_WRITER_PAYLOAD_DIGEST');\n    return [{ json: { ...r, lease_class: leaseClass, ttl_seconds: ttl, attempt_count: Number(r.attempt_count), admission, payload_sha256: String(r.payload_sha256).toLowerCase() } }];\n}\nif (!/^[0-9a-f-]{36}$/i.test(String(r.lease_id || '')) || !Number.isSafeInteger(r.fencing_token) || Number(r.fencing_token) <= 0)\n    throw new Error('INVALID_LEASE_TOKEN');\nif (op === 'RELEASE' && leaseClass === 'ACTUAL_OUTBOX') {\n    if (!/^[A-Za-z0-9:_-]{1,160}$/.test(String(r.outbox_id || '')) || !/^[A-Za-z0-9:_-]{1,160}$/.test(String(r.account_id || '')) || !/^[a-f0-9]{64}$/i.test(String(r.payload_sha256 || '')))\n        throw new Error('INVALID_RELEASE_CORRELATION');\n}\nreturn [{ json: { ...r, lease_class: leaseClass, fencing_token: Number(r.fencing_token), payload_sha256: r.payload_sha256 ? String(r.payload_sha256).toLowerCase() : r.payload_sha256 } }];\n"
      }
    },
    {
      "id": "18003",
      "name": "Lease Operation",
      "type": "n8n-nodes-base.switch",
      "typeVersion": 3.2,
      "position": [
        -520,
        0
      ],
      "parameters": {
        "rules": {
          "values": [
            {
              "conditions": {
                "options": {
                  "caseSensitive": true,
                  "typeValidation": "strict"
                },
                "combinator": "and",
                "conditions": [
                  {
                    "leftValue": "={{ $json.operation }}",
                    "rightValue": "ACQUIRE",
                    "operator": {
                      "type": "string",
                      "operation": "equals"
                    }
                  }
                ]
              }
            },
            {
              "conditions": {
                "options": {
                  "caseSensitive": true,
                  "typeValidation": "strict"
                },
                "combinator": "and",
                "conditions": [
                  {
                    "leftValue": "={{ $json.operation }}",
                    "rightValue": "ASSERT",
                    "operator": {
                      "type": "string",
                      "operation": "equals"
                    }
                  }
                ]
              }
            },
            {
              "conditions": {
                "options": {
                  "caseSensitive": true,
                  "typeValidation": "strict"
                },
                "combinator": "and",
                "conditions": [
                  {
                    "leftValue": "={{ $json.operation }}",
                    "rightValue": "RELEASE",
                    "operator": {
                      "type": "string",
                      "operation": "equals"
                    }
                  }
                ]
              }
            }
          ]
        },
        "options": {
          "fallbackOutput": "none"
        }
      }
    },
    {
      "id": "18004",
      "name": "Atomic Acquire Writer Lease",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [
        -220,
        0
      ],
      "alwaysOutputData": true,
      "parameters": {
        "operation": "executeQuery",
        "query": "WITH terminal AS (SELECT EXISTS (SELECT 1 FROM finance_ops.actual_writer_effects WHERE resource_key = $1::text AND outbox_id = $4::text AND account_id = $6::text AND budget_id = $8::text AND payload_sha256 = $7::text AND period_start = $9::date AND period_end = $10::date AND verified_payload_sha256 IS NOT NULL AND state IN ('VERIFIED', 'RECONCILED', 'COMMITTED')) AS present), resumable AS (SELECT EXISTS (SELECT 1 FROM finance_ops.actual_writer_effects WHERE resource_key = $1::text AND outbox_id = $4::text AND account_id = $6::text AND budget_id = $8::text AND payload_sha256 = $7::text AND period_start = $9::date AND period_end = $10::date AND state = 'ISSUED' AND $11::text = 'ACTUAL_OBSERVED') AS present), blockers AS (SELECT 1 FROM finance_ops.actual_writer_effects WHERE resource_key = $1::text AND budget_id = $8::text AND state IN ('PREPARED', 'ISSUED', 'ACTUAL_OBSERVED', 'OUTCOME_UNKNOWN') AND NOT ((state = 'PREPARED' AND outbox_id = $4::text AND attempt_count = 0 AND $5::text = 'INITIAL') OR (state = 'ISSUED' AND outbox_id = $4::text AND account_id = $6::text AND payload_sha256 = $7::text AND period_start = $9::date AND period_end = $10::date AND $5::text = 'SUCCESSOR' AND $11::text = 'ACTUAL_OBSERVED'))), acquired AS (SELECT lease.*, $4::text AS outbox_id FROM terminal CROSS JOIN resumable CROSS JOIN LATERAL finance_ops.acquire_writer_lease($1::text, $2::text, $3::integer) AS lease WHERE $13::text = 'MAINTENANCE' OR ($13::text = 'ACTUAL_OUTBOX' AND NOT EXISTS (SELECT 1 FROM blockers) AND (($5::text = 'INITIAL' AND $11::text = 'PREPARED' AND $12::integer = 0) OR ($5::text = 'SUCCESSOR' AND (terminal.present OR resumable.present))))), marked AS (INSERT INTO finance_ops.actual_writer_effects (resource_key, outbox_id, account_id, budget_id, payload_sha256, period_start, period_end, state, attempt_count, lease_id, lease_owner, fencing_token, issued_at, updated_at) SELECT $1::text, $4::text, $6::text, $8::text, $7::text, $9::date, $10::date, 'ISSUED', 1, lease_id, lease_owner, fencing_token, clock_timestamp(), clock_timestamp() FROM acquired WHERE $13::text = 'ACTUAL_OUTBOX' ON CONFLICT (resource_key, outbox_id) DO UPDATE SET state = CASE WHEN actual_writer_effects.state = 'PREPARED' THEN 'ISSUED' ELSE actual_writer_effects.state END, account_id = EXCLUDED.account_id, budget_id = EXCLUDED.budget_id, payload_sha256 = EXCLUDED.payload_sha256, period_start = EXCLUDED.period_start, period_end = EXCLUDED.period_end, lease_id = EXCLUDED.lease_id, lease_owner = EXCLUDED.lease_owner, fencing_token = EXCLUDED.fencing_token, attempt_count = actual_writer_effects.attempt_count + 1, issued_at = clock_timestamp(), updated_at = clock_timestamp() WHERE ($5::text = 'INITIAL' AND actual_writer_effects.state = 'PREPARED' AND actual_writer_effects.attempt_count = 0) OR ($5::text = 'SUCCESSOR' AND ((actual_writer_effects.state IN ('VERIFIED', 'RECONCILED', 'COMMITTED') AND actual_writer_effects.verified_payload_sha256 IS NOT NULL) OR (actual_writer_effects.state = 'ISSUED' AND $11::text = 'ACTUAL_OBSERVED')) AND actual_writer_effects.resource_key = $1::text AND actual_writer_effects.outbox_id = $4::text AND actual_writer_effects.account_id = $6::text AND actual_writer_effects.budget_id = $8::text AND actual_writer_effects.payload_sha256 = $7::text AND actual_writer_effects.period_start = $9::date AND actual_writer_effects.period_end = $10::date) RETURNING resource_key, outbox_id) SELECT acquired.* FROM acquired LEFT JOIN marked USING (resource_key, outbox_id) WHERE $13::text = 'MAINTENANCE' OR marked.resource_key IS NOT NULL;",
        "options": {
          "queryReplacement": "={{ [$json.resource_key, $json.lease_owner, $json.ttl_seconds, $json.outbox_id, $json.admission, $json.account_id, $json.payload_sha256, $json.budget_id, $json.period_start, $json.period_end, $json.outbox_state, $json.attempt_count, $json.lease_class] }}"
        }
      },
      "credentials": {
        "postgres": {
          "id": "BIND_FINANCE_OPS_DB",
          "name": "Finance Operations Postgres"
        }
      }
    },
    {
      "id": "18005",
      "name": "Assert Current Writer Fence",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [
        80,
        0
      ],
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT finance_ops.assert_writer_lease($1::text, $2::uuid, $3::bigint) AS valid;",
        "options": {
          "queryReplacement": "={{ [$json.resource_key, $json.lease_id, $json.fencing_token] }}"
        }
      },
      "credentials": {
        "postgres": {
          "id": "BIND_FINANCE_OPS_DB",
          "name": "Finance Operations Postgres"
        }
      }
    },
    {
      "id": "18006",
      "name": "Release Exact Writer Fence",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [
        380,
        0
      ],
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT CASE WHEN $4::text = 'MAINTENANCE' THEN finance_ops.release_maintenance_lease($1::text, $2::uuid, $3::bigint) ELSE finance_ops.release_writer_lease($1::text, $2::uuid, $3::bigint) END AS released;",
        "options": {
          "queryReplacement": "={{ [$json.resource_key, $json.lease_id, $json.fencing_token, $json.lease_class] }}"
        }
      },
      "credentials": {
        "postgres": {
          "id": "BIND_FINANCE_OPS_DB",
          "name": "Finance Operations Postgres"
        }
      }
    },
    {
      "id": "18007",
      "name": "Validate Lease Result",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        680,
        0
      ],
      "parameters": {
        "jsCode": "// Purpose: Validate Lease Result. Keep this deterministic and fail closed.\nconst req = $('Validate Fixed Lease Operation').first().json, row = $json;\nif (req.operation === 'ACQUIRE') {\n    if (!row.lease_id || Number(row.fencing_token) <= 0 || new Date(row.expires_at) <= new Date())\n        throw new Error('WRITER_LEASE_BUSY_OR_INVALID');\n    return [{ json: { operation: 'ACQUIRE', lease_class: req.lease_class, resource_key: String(row.resource_key), lease_id: String(row.lease_id), lease_owner: String(row.lease_owner), fencing_token: Number(row.fencing_token), expires_at: String(row.expires_at) } }];\n}\nif (req.operation === 'ASSERT' && row.valid !== true)\n    throw new Error('WRITER_LEASE_STALE');\nif (req.operation === 'RELEASE' && row.released !== true)\n    throw new Error('WRITER_LEASE_RELEASE_FAILED');\nreturn [{ json: { operation: req.operation, lease_class: req.lease_class, resource_key: req.resource_key, lease_id: req.lease_id, fencing_token: req.fencing_token, valid: row.valid === true, released: row.released === true } }];\n"
      }
    }
  ],
  "connections": {
    "Trusted Lease Request": {
      "main": [
        [
          {
            "node": "Validate Fixed Lease Operation",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Validate Fixed Lease Operation": {
      "main": [
        [
          {
            "node": "Lease Operation",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Lease Operation": {
      "main": [
        [
          {
            "node": "Atomic Acquire Writer Lease",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Assert Current Writer Fence",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Release Exact Writer Fence",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Atomic Acquire Writer Lease": {
      "main": [
        [
          {
            "node": "Validate Lease Result",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Assert Current Writer Fence": {
      "main": [
        [
          {
            "node": "Validate Lease Result",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Release Exact Writer Fence": {
      "main": [
        [
          {
            "node": "Validate Lease Result",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "timezone": "Asia/Dubai",
    "saveDataErrorExecution": "none",
    "saveDataSuccessExecution": "none",
    "errorWorkflow": "10000000-0000-4000-8000-000000000016"
  },
  "pinData": {},
  "meta": {
    "durableStateTable": "finance_ops.actual_writer_effects",
    "releaseEvidenceTable": "finance_ops.actual_writer_releases",
    "admissionPolicy": "PREPARED attempt zero, exact ACTUAL_OBSERVED over ISSUED, or terminal VERIFIED/RECONCILED/COMMITTED",
    "fenceReleasePolicy": "ACTUAL_OUTBOX requires durable COMMITTED readback; MAINTENANCE uses exact generic release",
    "financeWorkflowCode": "FINANCE_WRITER_LEASE",
    "migrationStatus": "SPEC_ONLY",
    "fixedSqlOnly": true,
    "callerSqlForbidden": true,
    "terminalReceiptReadback": true,
    "importTested": false,
    "fixtureExecuted": false,
    "credentialBindings": [
      {
        "placeholder": "BIND_FINANCE_OPS_DB",
        "configured": false,
        "action_required": true
      }
    ],
    "setupRequired": true,
    "workflowFolder": {
      "id": "f1000000-0000-4000-8000-000000000103",
      "name": "Shared",
      "placement": "POST_IMPORT_REVIEWED_MIGRATION"
    },
    "workflowTags": [
      "finance",
      "setup-required",
      "inactive"
    ],
    "leaseClasses": [
      "ACTUAL_OUTBOX",
      "MAINTENANCE"
    ]
  },
  "nodeGroups": [
    {
      "name": "Stage 1: Validate Fixed Lease Operation to Validate Lease Result",
      "nodeIds": [
        "18002",
        "18003",
        "18004",
        "18005",
        "18006",
        "18007"
      ],
      "description": "Finance stage 1. Input starts at Validate Fixed Lease Operation; verified output leaves through Validate Lease Result."
    }
  ],
  "tags": [
    {
      "id": "fin0000000000001",
      "name": "finance"
    },
    {
      "id": "fin0000000000002",
      "name": "setup-required"
    },
    {
      "id": "fin0000000000003",
      "name": "inactive"
    }
  ]
}
