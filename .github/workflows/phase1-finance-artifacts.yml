name: Phase 1 finance artifacts

on:
  pull_request:
    paths:
      - "packages/n8n-nodes-finance/**"
      - "tests/test_n8n_custom_image.py"
      - "services/pdf-utility/**"
      - "services/n8n-task-runners/**"
      - "tests/test_n8n_task_runners_image.py"
      - "config/ai-policies.json"
      - "integrations/n8n/contracts/ai-proposal-v1.schema.json"
      - "integrations/n8n/generated/ai-policy-contracts.seed.json"
      - ".github/workflows/phase1-finance-artifacts.yml"
  push:
    branches: [main]
    paths:
      - "packages/n8n-nodes-finance/**"
      - "tests/test_n8n_custom_image.py"
      - "services/pdf-utility/**"
      - "services/n8n-task-runners/**"
      - "tests/test_n8n_task_runners_image.py"
      - "config/ai-policies.json"
      - "integrations/n8n/contracts/ai-proposal-v1.schema.json"
      - "integrations/n8n/generated/ai-policy-contracts.seed.json"
      - ".github/workflows/phase1-finance-artifacts.yml"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  custom-nodes:
    runs-on: ubuntu-latest
    timeout-minutes: 50
    steps:
      - uses: actions/checkout@v5
      - name: Reclaim unused hosted toolchains for image export
        run: |
          df -h /
          # This disposable job needs Node and Docker, not Android or .NET.
          # Leave the hosted tool cache intact for actions/setup-node.
          sudo rm -rf /usr/local/lib/android /usr/share/dotnet
          df -h /
      - name: Configure ephemeral swap for image export
        shell: bash
        run: |
          set -Eeuo pipefail
          swap_file="$(mktemp "$RUNNER_TEMP/finance-nodes-swap.XXXXXX")"
          chmod 600 "$swap_file"
          fallocate -l 8G "$swap_file"
          mkswap "$swap_file"
          sudo swapon "$swap_file"
          sudo swapon --show=NAME --noheadings | grep -Fx "$swap_file"
          free -h
      - uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: packages/n8n-nodes-finance/package-lock.json
      - name: Install locked node package
        working-directory: packages/n8n-nodes-finance
        run: npm ci
      - name: Test and audit node package
        working-directory: packages/n8n-nodes-finance
        run: |
          npm test
          npm audit --audit-level=high
          npm pack --dry-run
          cd community-ai
          npm ci --omit=dev --ignore-scripts --legacy-peer-deps
          npm audit --omit=dev --audit-level=high
          test "$(node -p "require('./node_modules/n8n-nodes-prodex/package.json').version")" = "0.5.1"
      - name: Authenticate pinned base image pull
        env:
          GHCR_TOKEN: ${{ github.token }}
          GHCR_USER: ${{ github.actor }}
        run: printf '%s' "$GHCR_TOKEN" | docker login ghcr.io -u "$GHCR_USER" --password-stdin
      - name: Build exact custom n8n image
        id: build_custom_image
        timeout-minutes: 25
        run: |
          # These host installs have already passed tests and audits. Docker
          # installs the locked dependencies inside its own builder stage.
          rm -rf packages/n8n-nodes-finance/node_modules packages/n8n-nodes-finance/community-ai/node_modules
          df -h /
          docker system df
          heartbeat_pid=""
          stop_resource_heartbeat() {
            if [ -n "${heartbeat_pid:-}" ]; then
              kill "$heartbeat_pid" 2>/dev/null || true
              wait "$heartbeat_pid" 2>/dev/null || true
            fi
            echo "final resource diagnostics: $(date -u +'%Y-%m-%dT%H:%M:%SZ')"
            free -h || true
            df -h / || true
            ps -eo pid,ppid,stat,%cpu,%mem,rss,comm --sort=-rss | sed -n '1,20p' || true
            echo "final kernel OOM diagnostics"
            sudo dmesg --ctime 2>/dev/null | grep -Ei 'out of memory|oom-kill|killed process' | tail -n 20 || true
          }
          trap stop_resource_heartbeat EXIT
          (
            while sleep 30; do
              echo "resource heartbeat: $(date -u +'%Y-%m-%dT%H:%M:%SZ')"
              free -h
              df -h /
              ps -eo pid,ppid,stat,%cpu,%mem,rss,comm --sort=-rss | sed -n '1,20p'
            done
          ) &
          heartbeat_pid=$!
          docker build --build-arg FINANCE_SOURCE_COMMIT="${{ github.sha }}" --file packages/n8n-nodes-finance/Dockerfile.n8n --tag finance-n8n:${{ github.sha }} .
      - name: Smoke custom node image
        run: |
          echo "custom smoke checkpoint: inspect image and version"
          test "$(docker image inspect finance-n8n:${{ github.sha }} --format '{{ index .Config.Labels "org.opencontainers.image.revision" }}')" = "${{ github.sha }}"
          test "$(docker run --rm --entrypoint n8n finance-n8n:${{ github.sha }} --version)" = "2.36.2"
          docker run --rm --entrypoint node finance-n8n:${{ github.sha }} -e "const root='/opt/finance-n8n/custom-extensions/n8n-nodes-finance'; const p=require(root+'/package.json'); if(p.version!=='0.1.0'||process.env.N8N_CUSTOM_EXTENSIONS!==undefined) process.exit(1); for(const f of ['ActualBudget/ActualBudget.node.js','FinancePdf/FinancePdf.node.js','FinanceRules/FinanceRules.node.js','FinanceStatement/FinanceStatement.node.js']) require(root+'/dist/nodes/'+f); for(const f of ['ActualBudgetApi.credentials.js','FinanceStatementPassword.credentials.js']) require(root+'/dist/credentials/'+f);"
          docker run --rm --entrypoint node finance-n8n:${{ github.sha }} --input-type=module -e "if (process.cwd() !== '/home/node') process.exit(1); import { Codex } from '@openai/codex-sdk'; if (typeof Codex !== 'function') process.exit(1);"
          echo "custom smoke checkpoint: reclaim builder cache"
          docker image inspect finance-n8n:${{ github.sha }} --format 'image size={{.Size}} bytes'
          df -h / "$RUNNER_TEMP"
          docker system df
          docker builder prune --all --force
          df -h / "$RUNNER_TEMP"
          docker system df
          assert_rejected() {
            expected_message="$1"
            shift
            set +e
            output="$("$@" 2>&1)"
            status=$?
            set -e
            if [ "$status" -ne 1 ]; then
              echo "Expected rejection exit code 1, got $status" >&2
              printf '%s\n' "$output" >&2
              return 1
            fi
            printf '%s\n' "$output" | grep -Fqx "$expected_message"
          }
          echo "custom smoke checkpoint: reject mutable ProDex SDK path"
          assert_rejected \
            'FINANCE_EXTENSION_MUTABLE_PATH_REJECTED: unexpected symlink target' \
            docker run --rm --entrypoint sh finance-n8n:${{ github.sha }} \
            -c 'rm /home/node/node_modules && ln -s /tmp /home/node/node_modules && /opt/finance-n8n/finance-entrypoint.sh --version'
          state_dir="$(mktemp -d)"
          cleanup_state_dir() {
            if [ -n "${state_dir:-}" ]; then
              sudo rm -rf -- "$state_dir"
            fi
          }
          trap cleanup_state_dir EXIT
          chmod 0777 "$state_dir"
          docker run --rm -v "$state_dir:/home/node/.n8n" finance-n8n:${{ github.sha }} --version
          docker run --rm -v "$state_dir:/home/node/.n8n" finance-n8n:${{ github.sha }} --version
          test "$(readlink "$state_dir/nodes/node_modules/n8n-nodes-finance")" = "/opt/finance-n8n/custom-extensions/n8n-nodes-finance"
          test "$(readlink "$state_dir/nodes/node_modules/n8n-nodes-prodex")" = "/opt/finance-n8n/community-extensions/node_modules/n8n-nodes-prodex"
          docker run --rm \
            --entrypoint node \
            -v "$state_dir:/home/node/.n8n" \
            --tmpfs /tmp:uid=1000,gid=1000,mode=1777 \
            finance-n8n:${{ github.sha }} \
            /opt/finance-n8n/assert-runtime-registration.cjs export:nodes --output=/tmp/node-types.json | \
            grep -Fx 'finance extension registration verified: 7 nodes, 3 credentials'
          echo "custom smoke checkpoint: reject API-key-bearing community runtime"
          assert_rejected \
            'COMMUNITY_AI_API_KEY_FORBIDDEN:OPENAI_API_KEY' \
            docker run --rm -e OPENAI_API_KEY=forbidden -v "$state_dir:/home/node/.n8n" finance-n8n:${{ github.sha }} --version
          docker run --rm \
            -v "$state_dir:/home/node/.n8n" \
            --entrypoint sh \
            finance-n8n:${{ github.sha }} \
            -c "rm /home/node/.n8n/nodes/node_modules/n8n-nodes-finance && mkdir /home/node/.n8n/nodes/node_modules/n8n-nodes-finance"
          echo "custom smoke checkpoint: reject mutable finance package path"
          assert_rejected \
            'FINANCE_EXTENSION_MUTABLE_PATH_REJECTED: expected absent path or exact symlink' \
            docker run --rm -v "$state_dir:/home/node/.n8n" finance-n8n:${{ github.sha }} --version
          echo "custom smoke checkpoint: verify nodemailer overlay"
          docker run --rm --entrypoint node finance-n8n:${{ github.sha }} -e "const n=require('/usr/local/lib/node_modules/n8n/node_modules/nodemailer'); const p=require('/usr/local/lib/node_modules/n8n/node_modules/nodemailer/package.json'); if(p.version!=='9.0.1'||typeof n.createTransport({jsonTransport:true}).sendMail!=='function') process.exit(1);"
      - name: Scan custom n8n image
        if: ${{ !cancelled() && steps.build_custom_image.outcome == 'success' }}
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: finance-n8n:${{ github.sha }}
          format: table
          severity: HIGH,CRITICAL
          ignore-unfixed: false
          exit-code: "1"
      - name: Generate custom n8n SBOM
        uses: anchore/sbom-action@f8bdd1d8ac5e901a77a92f111440fdb1b593736b # v0.20.6
        with:
          image: finance-n8n:${{ github.sha }}
          artifact-name: finance-n8n-${{ github.sha }}.spdx.json
      - name: Publish immutable custom n8n candidate
        if: github.event_name == 'push'
        run: |
          echo "${{ github.token }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
          image="ghcr.io/${{ github.repository_owner }}/finance-n8n:${{ github.sha }}"
          docker tag "finance-n8n:${{ github.sha }}" "$image"
          docker push "$image"
          immutable_ref="$(docker image inspect "$image" --format '{{index .RepoDigests 0}}')"
          test -n "$immutable_ref"
          jq -n --arg image "$image" --arg immutable_ref "$immutable_ref" --arg source_sha "${{ github.sha }}" '{schema_version:1,image:$image,immutable_ref:$immutable_ref,source_sha:$source_sha}' > finance-n8n-image-receipt.json
      - name: Upload custom n8n digest receipt
        if: github.event_name == 'push'
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: finance-n8n-image-receipt-${{ github.sha }}
          path: finance-n8n-image-receipt.json
          if-no-files-found: error

  pdf-utility:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: services/pdf-utility/requirements.txt
      - name: Install pinned PDF dependencies
        working-directory: services/pdf-utility
        run: python -m pip install --requirement requirements.txt
      - name: Test PDF utility on Linux UDS
        working-directory: services/pdf-utility
        run: python -m unittest discover -s tests -v
      - name: Audit Python dependencies
        run: |
          python -m pip install pip-audit==2.9.0
          python -m pip_audit --requirement services/pdf-utility/requirements.txt --no-deps --disable-pip
      - name: Build PDF utility image
        run: docker build --tag finance-pdf-utility:${{ github.sha }} services/pdf-utility
      - name: Smoke sandbox and network denial
        run: |
          docker run --rm --network none --read-only --tmpfs /tmp:size=96m,mode=1770,uid=1000,gid=1000 --tmpfs /run/platform-pdf:size=4m,mode=0770,uid=1000,gid=1000 --entrypoint python finance-pdf-utility:${{ github.sha }} -m unittest discover -s /opt/platform-pdf/tests -v
          docker run --rm --network none --read-only --tmpfs /tmp:size=4m --entrypoint python finance-pdf-utility:${{ github.sha }} /opt/platform-pdf/tests/network_probe.py
      - name: Scan PDF utility image
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: finance-pdf-utility:${{ github.sha }}
          format: table
          severity: HIGH,CRITICAL
          ignore-unfixed: false
          exit-code: "1"
      - name: Generate PDF utility SBOM
        uses: anchore/sbom-action@f8bdd1d8ac5e901a77a92f111440fdb1b593736b # v0.20.6
        with:
          image: finance-pdf-utility:${{ github.sha }}
          artifact-name: finance-pdf-utility-${{ github.sha }}.spdx.json
      - name: Publish immutable PDF utility candidate
        if: github.event_name == 'push'
        run: |
          echo "${{ github.token }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
          image="ghcr.io/${{ github.repository_owner }}/finance-pdf-utility:${{ github.sha }}"
          docker tag "finance-pdf-utility:${{ github.sha }}" "$image"
          docker push "$image"
          immutable_ref="$(docker image inspect "$image" --format '{{index .RepoDigests 0}}')"
          test -n "$immutable_ref"
          jq -n --arg image "$image" --arg immutable_ref "$immutable_ref" --arg source_sha "${{ github.sha }}" '{schema_version:1,image:$image,immutable_ref:$immutable_ref,source_sha:$source_sha}' > finance-pdf-utility-image-receipt.json
      - name: Upload PDF utility digest receipt
        if: github.event_name == 'push'
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: finance-pdf-utility-image-receipt-${{ github.sha }}
          path: finance-pdf-utility-image-receipt.json
          if-no-files-found: error

  n8n-task-runners:
    runs-on: ubuntu-latest
    timeout-minutes: 50
    steps:
      - name: Checkout finance repository
        uses: actions/checkout@v5
      - name: Checkout exact n8n source
        uses: actions/checkout@v5
        with:
          repository: n8n-io/n8n
          ref: bc9090e8c61d0dc84aa85528e62142dfb7001243
          path: .upstream/n8n
          fetch-depth: 1
      - name: Checkout exact launcher source
        uses: actions/checkout@v5
        with:
          repository: n8n-io/task-runner-launcher
          ref: fad71fc0dab52a6b6558765d1431c7846cfd816f
          path: .upstream/task-runner-launcher
          fetch-depth: 1
      - name: Verify upstream commits and apply security-only source update
        shell: bash
        run: |
          python -m unittest tests.test_n8n_task_runners_image tests.test_n8n_runner_closure -v
          test "$(git -C .upstream/n8n rev-parse HEAD)" = "bc9090e8c61d0dc84aa85528e62142dfb7001243"
          test "$(git -C .upstream/task-runner-launcher rev-parse HEAD)" = "fad71fc0dab52a6b6558765d1431c7846cfd816f"
          python services/n8n-task-runners/patch_launcher.py .upstream/task-runner-launcher
          test "$(git -C .upstream/task-runner-launcher diff --name-only)" = $'go.mod\ngo.sum'
          grep -Fx $'\tgolang.org/x/text v0.39.0 // indirect' .upstream/task-runner-launcher/go.mod
      - uses: actions/setup-node@v5
        with:
          node-version: "24.18.1"
      - name: Build exact JavaScript runner dependency closure
        working-directory: .upstream/n8n
        shell: bash
        run: |
          corepack enable
          corepack prepare pnpm@10.32.1 --activate
          pnpm install --frozen-lockfile
          pnpm --filter=@n8n/task-runner... build
          rm -rf dist/task-runner-javascript
          NODE_ENV=production DOCKER_BUILD=true pnpm --filter=@n8n/task-runner --prod --legacy deploy --no-optional dist/task-runner-javascript
          test "$(node -p "require('./dist/task-runner-javascript/package.json').version")" = "2.36.1"
          test -f dist/task-runner-javascript/dist/start.js
          test -f dist/task-runner-javascript/node_modules/@n8n/di/package.json
          closure=dist/task-runner-javascript
          extras="$(mktemp -d)"
          cp \
            ../../services/n8n-task-runners/javascript-extras/package.json \
            ../../services/n8n-task-runners/javascript-extras/package-lock.json \
            "$extras/"
          npm ci --prefix "$extras" --ignore-scripts --omit=dev --fund=false --audit=false
          test "$(node -p "require('$extras/node_modules/moment/package.json').version")" = "2.30.1"
          cp -a "$extras/node_modules/moment" "$closure/node_modules/moment"
          python ../../services/n8n-task-runners/relink_closure.py "$closure" .
          cp ../../services/n8n-task-runners/verify_workspace_packages.mjs "$closure/finance-verify-workspace-packages.mjs"
          python ../../services/n8n-task-runners/workspace_package_manifest.py \
            "$closure" . \
            --output "$closure/finance-workspace-packages.json"
          node "$closure/finance-verify-workspace-packages.mjs" "$closure"
          python ../../services/n8n-task-runners/validate_closure.py "$closure"
          test "$(jq -r .schema_version "$closure/finance-closure-manifest.json")" = "1"
          test "$(jq -r .relinked "$closure/finance-closure-relinks.json")" = "0"
          sha256sum \
            ../../services/n8n-task-runners/relink_closure.py \
            ../../services/n8n-task-runners/validate_closure.py \
            ../../services/n8n-task-runners/workspace_package_manifest.py \
            ../../services/n8n-task-runners/verify_workspace_packages.mjs \
            ../../services/n8n-task-runners/javascript-extras/package-lock.json
      - name: Build source-reproduced task runners image
        run: >-
          docker build
          --build-arg FINANCE_SOURCE_COMMIT="${{ github.sha }}"
          --file services/n8n-task-runners/Dockerfile
          --tag finance-n8n-task-runners:${{ github.sha }} .
      - name: Smoke exact runtime and n8n broker protocol
        shell: bash
        run: |
          test "$(docker run --rm --entrypoint node finance-n8n-task-runners:${{ github.sha }} --version)" = "v24.18.1"
          docker run --rm --entrypoint /opt/runners/task-runner-python/.venv/bin/python finance-n8n-task-runners:${{ github.sha }} -I -B -X disable_remote_debug -c "import src.main, urllib3, websockets"
          docker run --rm --entrypoint python finance-n8n-task-runners:${{ github.sha }} -c "import importlib.metadata as m; names={d.metadata['Name'].lower() for d in m.distributions()}; assert not names.intersection({'pip','setuptools','msgpack'}), names"
          docker run --rm --entrypoint node finance-n8n-task-runners:${{ github.sha }} /opt/runners/task-runner-javascript/finance-verify-workspace-packages.mjs /opt/runners/task-runner-javascript
          source_closure_sha="$(jq -r .closure_sha256 .upstream/n8n/dist/task-runner-javascript/finance-closure-manifest.json)"
          test "$source_closure_sha" != "null"
          test -n "$source_closure_sha"
          printf '%s\n' "$source_closure_sha" > finance-n8n-task-runners-closure-sha256.txt
          copied_closure="$RUNNER_TEMP/finance-n8n-task-runners-image-closure"
          test ! -e "$copied_closure"
          mkdir -- "$copied_closure"
          closure_container="$(docker create finance-n8n-task-runners:${{ github.sha }})"
          docker cp "$closure_container:/opt/runners/task-runner-javascript/." "$copied_closure/"
          docker rm "$closure_container"
          image_closure_sha="$(python services/n8n-task-runners/validate_closure.py "$copied_closure" --manifest "$RUNNER_TEMP/image-closure-manifest.json" | jq -r .closure_sha256)"
          test "$image_closure_sha" = "$source_closure_sha"
          test "$(realpath "$copied_closure")" = "$(realpath "$RUNNER_TEMP")/finance-n8n-task-runners-image-closure"
          rm -rf -- "$copied_closure"
          test "$(docker image inspect finance-n8n-task-runners:${{ github.sha }} --format '{{ index .Config.Labels "finance.n8n.source_commit" }}')" = "bc9090e8c61d0dc84aa85528e62142dfb7001243"
          test "$(docker image inspect finance-n8n-task-runners:${{ github.sha }} --format '{{ index .Config.Labels "finance.n8n.source_commit" }}')" != "UNVERIFIED"
          test "$(docker image inspect finance-n8n-task-runners:${{ github.sha }} --format '{{ index .Config.Labels "finance.finance_source_commit" }}')" = "${{ github.sha }}"
          test "$(docker image inspect finance-n8n-task-runners:${{ github.sha }} --format '{{ index .Config.Labels "finance.finance_source_commit" }}')" != "UNVERIFIED"
          sh services/n8n-task-runners/protocol-smoke.sh \
            finance-n8n-task-runners:${{ github.sha }} \
            ghcr.io/n8n-io/n8n:2.36.2@sha256:be13ef936c03ce0f2d58426afa06e7f1ba2a1d50e4f19ebf3e8488435bf5e386
      - name: Reclaim task-runner build space before security analysis
        shell: bash
        run: |
          set -euo pipefail
          test -s finance-n8n-task-runners-closure-sha256.txt
          workspace="$(pwd -P)"
          n8n_source="$(realpath .upstream/n8n)"
          launcher_source="$(realpath .upstream/task-runner-launcher)"
          test "$n8n_source" = "$workspace/.upstream/n8n"
          test "$launcher_source" = "$workspace/.upstream/task-runner-launcher"
          echo "Disk budget before task-runner cleanup"
          df -h / "$RUNNER_TEMP"
          docker system df
          docker image rm \
            ghcr.io/n8n-io/n8n:2.36.2@sha256:be13ef936c03ce0f2d58426afa06e7f1ba2a1d50e4f19ebf3e8488435bf5e386 \
            >/dev/null 2>&1 || true
          rm -rf -- "$n8n_source" "$launcher_source"
          docker builder prune --all --force
          docker image inspect finance-n8n-task-runners:${{ github.sha }} >/dev/null
          echo "Disk budget after task-runner cleanup"
          df -h / "$RUNNER_TEMP"
          docker system df
      - name: Scan task runners image
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: finance-n8n-task-runners:${{ github.sha }}
          format: table
          severity: HIGH,CRITICAL
          ignore-unfixed: false
          exit-code: "1"
      - name: Generate task runners SBOM
        uses: anchore/sbom-action@f8bdd1d8ac5e901a77a92f111440fdb1b593736b # v0.20.6
        with:
          image: finance-n8n-task-runners:${{ github.sha }}
          artifact-name: finance-n8n-task-runners-${{ github.sha }}.spdx.json
      - name: Publish immutable task runners candidate
        if: github.event_name == 'push'
        shell: bash
        run: |
          echo "${{ github.token }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
          image="ghcr.io/${{ github.repository_owner }}/finance-n8n-task-runners:${{ github.sha }}"
          docker tag "finance-n8n-task-runners:${{ github.sha }}" "$image"
          docker push "$image"
          immutable_ref="$(docker image inspect "$image" --format '{{index .RepoDigests 0}}')"
          test -n "$immutable_ref"
          closure_sha256="$(cat finance-n8n-task-runners-closure-sha256.txt)"
          test "$closure_sha256" != "null"
          test -n "$closure_sha256"
          relinker_sha256="$(sha256sum services/n8n-task-runners/relink_closure.py | cut -d' ' -f1)"
          validator_sha256="$(sha256sum services/n8n-task-runners/validate_closure.py | cut -d' ' -f1)"
          workspace_manifest_builder_sha256="$(sha256sum services/n8n-task-runners/workspace_package_manifest.py | cut -d' ' -f1)"
          workspace_verifier_sha256="$(sha256sum services/n8n-task-runners/verify_workspace_packages.mjs | cut -d' ' -f1)"
          jq -n \
            --arg image "$image" \
            --arg immutable_ref "$immutable_ref" \
            --arg source_sha "${{ github.sha }}" \
            --arg n8n_source_commit "bc9090e8c61d0dc84aa85528e62142dfb7001243" \
            --arg launcher_source_commit "fad71fc0dab52a6b6558765d1431c7846cfd816f" \
            --arg closure_sha256 "$closure_sha256" \
            --arg relinker_sha256 "$relinker_sha256" \
            --arg validator_sha256 "$validator_sha256" \
            --arg workspace_manifest_builder_sha256 "$workspace_manifest_builder_sha256" \
            --arg workspace_verifier_sha256 "$workspace_verifier_sha256" \
            '{schema_version:1,image:$image,immutable_ref:$immutable_ref,source_sha:$source_sha,n8n_source_commit:$n8n_source_commit,launcher_source_commit:$launcher_source_commit,closure_sha256:$closure_sha256,relinker_sha256:$relinker_sha256,validator_sha256:$validator_sha256,workspace_manifest_builder_sha256:$workspace_manifest_builder_sha256,workspace_verifier_sha256:$workspace_verifier_sha256,security_overrides:{"golang.org/x/text":"v0.39.0"}}' \
            > finance-n8n-task-runners-image-receipt.json
      - name: Upload task runners digest receipt
        if: github.event_name == 'push'
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: finance-n8n-task-runners-image-receipt-${{ github.sha }}
          path: finance-n8n-task-runners-image-receipt.json
          if-no-files-found: error
