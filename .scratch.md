# W19 four-table stacked implementation

- Base: `c73f23ab636b038d08b3cf4672a9b729c127d201`
- Source commit: `f01df008241ed18964da8cf7feac826c6ba53f28`
- W19 now creates only the matrix targets in dependency order: ingestion state, documents, actual batches, AI reviews.
- Row seed/readback nodes and legacy fifteen-table schema creation are removed; W19 remains manual-only and idempotent.
- Matrix scanner validates target create schemas but excludes target nodes from the preserved legacy source inventory; source tables with no runtime refs are allowed.
- Approved contract/artifact uses `finance_ai_reviews`; bead text's `finance_ai_review_queue` is inconsistent with the matrix and project docs.
- `generate_platform_bootstrap.py` and its manifest are owned by the shared-core lane and remain unchanged here.
- Focused target/W19 tests pass with `uv run --with jsonschema --with pytest python -m pytest ...`.
